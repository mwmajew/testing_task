# Add tests using unittest here
# at least 10 tests
# create `your_partners_github_username.py` file and paste your partner's code there
# if it includes other files, add the files to this repo as well
# when you finished including your partner's code, commit it, and THEN start doing the task
# you code should be runnable with `python -m unittest task.py`

import your_partners_github_username
